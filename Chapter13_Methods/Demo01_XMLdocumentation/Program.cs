Console.Title = "Demo XML Documentation";


/// <summary>
/// Counts to the given number, starting at 1 and including the number provided
/// </summary>
void Count(int numberToCount)
{
    for (int idx = 1; idx <= numberToCount; idx++)
        Console.WriteLine(idx);
}